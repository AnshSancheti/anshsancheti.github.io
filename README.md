# Flip-disc panel experiment

A 28x28 interactive flip-disc display exploring a hand-drawn, chalkboard-style aesthetic for the personal site.

## Run locally

```sh
npm install
npm run dev
```

Use `?preview=linkedin`, `?preview=github`, or `?preview=email` to open directly to a social icon preview.
